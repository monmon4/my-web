<h1>Login</h1>

<%= form_for(:user, url: sessions_path) do |f| %>

	<div class="field"> <%= f.label :name %> <br/> <%= f.text_field :name %></div>
	<p/>
	<div class="field"><%= f.label :password %> <br/> <%= f.password_field :password %></div>
	<div class="field"><%=f.hidden_field :state, value: "login"%></div>
	<div class="action"><%= f.submit "Login" %> </div>
	<%end%>

<h1>Signup</h1>

<%= form_for(:user, url: sessions_path) do |f| %>

	<div class="field"> <%= f.label :name %> <br/> <%= f.text_field :name %></div>
	<p/>
	<div class="field"><%= f.label :password %> <br/> <%= f.password_field :password %></div>
	<div class="field"><%=f.hidden_field :state, value: "signup"%></div>
	<div class="action"><%= f.submit "Signup" %> </div>
	<%end%>